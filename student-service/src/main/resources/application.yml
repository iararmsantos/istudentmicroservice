server:
  port: 8082

spring:
  application:
    name: student-service
  #database configuration
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/student_service?useTimezone=true&serverTimezone=UTC
    username: root
    password: root

  jpa:
    show_sql: true
    hibernate:
      ddl-auto: update

#  flyway:
#    url: jdbc:mysql://localhost:3310
#    schemas: student-service
#    user: root
#    password: root
